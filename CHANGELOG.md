# Changelog

## [1.0.8] - 2026-05-11

- App Store packaging: bilingual `info.xml` (EN/DE), `<donation>`, `<navigations>`, expanded keywords, modern `<dependencies>` with PHP bounds, five screenshot slots on `main`.
- Added `LICENSE`, release `Makefile`, hardened `.gitignore` (signing keys, build output), and `screenshots/` placeholders — replace PNGs with real UI before marketing.
