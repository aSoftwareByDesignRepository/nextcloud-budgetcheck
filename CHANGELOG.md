# Changelog

## [1.0.8] - 2026-05-22

- App Store packaging: bilingual `info.xml` (EN/DE), `<donation>`, `<navigations>`, expanded keywords, modern `<dependencies>` with PHP bounds, eleven screenshot slots on `main`.
- Screenshots: `budgetcheck-screenshot-01.png` … `11.png` aligned with `info.xml` and public repo `main`.
- Added `LICENSE`, release `Makefile`, hardened `.gitignore` (signing keys, build output).
